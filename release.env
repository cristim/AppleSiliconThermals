# Release binary that setup.sh installs. After each tagged release, update VERSION and SHA256 to that
# release's apple-silicon-thermals-aarch64-linux-musl asset (the published .sha256 file).
VERSION=1.1.0
SHA256=PLACEHOLDER-NOT-YET-PINNED
